export default  ( state: boolean ) => {
    console.log( state );

    //const whiteElements = document.querySelectorAll( '[data-white]' );
    //console.log( document.querySelectorAll( '[data-white]' ) );
    // watch(target, () => {
    //     if ( target.value ) {
    //         document.querySelectorAll( '[data-white]' ).forEach( item => item.classList.add( item.dataset.white + '--white' ) );
    //     } else {
    //         document.querySelectorAll( '[data-white]' ).forEach( item => item.classList.remove( item.dataset.white + '--white' ) );
    //     }
    // });
}